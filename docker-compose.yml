services:
  chroma-mcp-server:
    build: .
    environment:
      - CHROMA_CLIENT_TYPE=persistent
      - CHROMA_DATA_DIR=/data
      - CHROMA_LOG_DIR=/logs
      - CHROMA_EMBEDDING_FUNCTION=default
    volumes:
      - ./chroma_data:/data
      - ./logs:/logs
    ports:
      - '8000:8000'
